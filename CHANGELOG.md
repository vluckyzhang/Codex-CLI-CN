# 更新日志

## v1.120.1

- 新增“检测安装路径”能力，综合 `where codex`、`npm prefix -g`、`npm root -g` 与常见 Windows 全局目录自动判断真实安装位置。
- 修复 GUI 中 `npm`/`node` 子进程在 Windows 图形进程下无法稳定拉起的问题，环境检查现在可正确返回状态。
- 修复 GUI 环境检测线程直接访问 `tkinter` 状态导致的无响应问题，并隐藏检测时弹出的黑色控制台窗口。
- GUI 默认请求管理员权限启动，减少补丁写入与沙箱相关操作中的权限失败。
- 扩大包装层覆盖范围，新增 `locate` 命令，并将登录提示、常用状态输出与共享说明文本纳入中文处理范围。
- 美化项目介绍页，新增 SVG 图片素材与 Emoji 视觉元素。
- 更新 Windows GUI 发行版流程，版本号提升为 `1.120.1`。

## v0.120.1

- 新增基于 Python + CustomTkinter 的 GUI 图形界面。
- 新增 GUI 自检入口、PyInstaller spec 与一键构建脚本。
- 新增 Windows GUI 发行版打包流程。
- 将 GUI“关于”说明、界面文案与日志处理统一为 UTF-8，修复乱码问题。
- 为补丁脚本补充 `--json` 输出，便于 GUI 与自动化工具调用。

## v0.120.0

- 新建 `Codex-CLI-CN` 仓库结构。
- 为 `@openai/codex 0.120.0` 设计新的中文兼容补丁方案。
- 新增自动备份、恢复、状态检查脚本。
- 新增 `0.120.0` 帮助页与说明文本翻译表。
- 新增中文 `README`、来源说明与静态介绍页。
- 明确标注原作者、原项目来源与官方上游仓库。
